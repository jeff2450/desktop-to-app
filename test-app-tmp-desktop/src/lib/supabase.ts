import { localApi } from "@/lib/localApi";

const supabaseUrl = undefined /* removed by WebToApp */;
const supabaseKey = undefined /* removed by WebToApp */;

export // Supabase client replaced by WebToApp local API
// const supabase = createClient(...) — removed;
