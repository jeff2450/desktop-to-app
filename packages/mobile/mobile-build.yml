# ⚠️  REFERENCE COPY — Do not edit this file directly.
#
# The canonical production workflow is:
#   .github/workflows/mobile-build.yml
#
# This file is kept as a reference for the original standalone workflow
# pattern. The version in .github/workflows/ is significantly upgraded with:
#
#   1. mobile-unit-tests  — Vitest suite for @webtoapp/mobile (every push/PR)
#   2. gradle-health-check — JDK 17 validation + ./gradlew --dry-run (every push/PR)
#   3. build-android      — Full APK build (main push + workflow_dispatch)
#   4. build-ios          — Simulator build (workflow_dispatch only)
#
# Additional improvements in the canonical workflow:
#   - pnpm + turbo cache (matching monorepo ci.yml patterns)
#   - Gradle dependency cache across runs
#   - JDK 17 version guard (fails if a different Java is on PATH)
#   - Android SDK license acceptance + SDK package installation
#   - GitHub Step Summary tables for build status
#   - Structured artifact uploads with SHA-stamped names (14-day retention)
#   - Separate Gradle + xcodebuild failure log uploads
#
# See: .github/workflows/mobile-build.yml
